{% extends 'base.html.twig' %}

{% block title %}Connexion :{% endblock %}

{% block body %}
<div class="container">
    <div class="w-75 mx-auto mt-5">
        <p class="text-center"><a class="nav-link text-dark" href="{{ path('app_forgot_password_request')}}">Modifier password</a></p>
        <form method="post" class="w-50 mx-auto p-2 mt-3">
            {% if error %}
                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}

            {% if app.user %}
                <div class="mb-3">
                    Vous êtes connecté en tant que {{ app.user.username }}, <a href="{{ path('app_logout') }}">Déconnexion</a>
                </div>
            {% endif %}

            <h1 class="h3 mb-3 font-weight-normal text-center">Veuillez vous connecter !</h1>
            <hr class="w-25 text-center bg-danger">
            <label for="inputEmail">Email</label>
            <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
            <label for="inputPassword">Password</label>
            <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>

            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

            {#
                Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                See https://symfony.com/doc/current/security/remember_me.html

                <div class="checkbox mb-3">
                    <label>
                        <input type="checkbox" name="_remember_me"> Remember me
                    </label>
                </div>
            #}

            <button class="btn btn-outline-info mt-3" type="submit">Connexion</button>
        </form>
    </div>
</div>
{% endblock %}
